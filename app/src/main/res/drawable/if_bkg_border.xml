<?xml version="1.0" encoding="UTF-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- Colored rectangle-->
    <item>
        <shape android:shape="rectangle">
            <size
                android:width="60dp"
                android:height="40dp" />
            <solid android:color="@color/if_color" />
        </shape>
    </item>

    <!-- These rectangles for the right side -->
    <!-- Their color should be the same as layout's -->
    <item
        android:top="-40dp"
        android:bottom="63dp"
        android:right="-30dp">
        <rotate
            android:fromDegrees="53">
            <shape android:shape="rectangle">
                <solid android:color="#ffffff" />
            </shape>
        </rotate>
    </item>

    <item
        android:top="63dp"
        android:bottom="-40dp"
        android:right="-30dp">
        <rotate
            android:fromDegrees="-53">
            <shape android:shape="rectangle">
                <solid android:color="#ffffff" />
            </shape>
        </rotate>
    </item>


    <!--border-->
    <item
        android:right="-8dp">
        <shape android:shape="rectangle">
            <corners android:radius="5dp" />
            <stroke android:color="@color/if_color" android:width="8dp" />
        </shape>
    </item>


    <!-- These rectangles are for the left side -->
    <!-- Their color should be the same as layout's -->
    <item
        android:top="-40dp"
        android:bottom="63dp"
        android:left="-30dp"
        >
        <rotate
            android:fromDegrees="127">
            <shape android:shape="rectangle" >
                <solid android:color="#ffffff" />
            </shape>
        </rotate>
    </item>

    <item
        android:top="63dp"
        android:bottom="-40dp"
        android:left="-30dp"
        >
        <rotate
            android:fromDegrees="-127">
            <shape android:shape="rectangle">
                <solid android:color="#ffffff" />
            </shape>
        </rotate>
    </item>


</layer-list>

